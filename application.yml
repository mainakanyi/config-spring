spring:
  application:
    name: TestApplication

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: '*'

hello:
  rel: bsjghdajsh
  
spring: 
  cloud: 
    config:
      uri: http://localhost:8888
      enabled: true